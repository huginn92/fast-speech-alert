services:
  - type: web
    name: speech-alert
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
    envVars:
      - key: GROK_API_KEY
        fromDatabase: true  # Set in Render dashboard
      - key: YOUR_EMAIL
        value: your@email.com
